- name: Build app
  run: npm run build -- --base-href /Map_Draw/

- name: Deploy
  uses: peaceiris/actions-gh-pages@v3
  with:
    github_token: ${{ secrets.GITHUB_TOKEN }}
    publish_dir: ./dist/map-draw   # ← adjust to your actual folder name